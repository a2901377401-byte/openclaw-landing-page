{
  "name": "openclaw-landing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@element-plus/icons-vue": "^2.3.0",
    "element-plus": "^2.5.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.32.2",
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^6.1.0",
    "wrangler": "^4.82.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
